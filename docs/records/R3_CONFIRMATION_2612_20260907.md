# R3 版本与维护者实机确认（NeoForge 26.1.2，2026-09-07）

## 1. 本轮授权与确认来源

维护者在本轮会话明确要求：

> 关于R3和实机测试PASS的表述也跟进，最后合并

据此把本线源码版本从 `1.1.8+neoforge.26.1.2.R2` 提升为
**`1.1.8+neoforge.26.1.2.R3`**，并将本线 R3 修复状态记为
**“维护者实机测试 PASS（2026-09-07，本轮确认）”**。

这是维护者对**本线 R3 修复的整体确认**，不是 AI 在沙箱运行 Minecraft 得到的结论，
也不是继续从 1.21.11 复制 PASS。本轮没有新增附件、硬件信息、枪包版本或逐场景日志，
因此不补造具体测试步骤、时长或逐用例通过记录。

## 2. 覆盖范围与未扩大事项

R3 集结的修复范围见本线 [`CHANGELOG`](../../CHANGELOG.md)：

- 共用脚本环境恢复 LuaJ 3.0.1 的 StringLib；
- 创造模式搜索树同步的防御性修复；
- 跨维度、服务器 tick、配置、重生、命中方块与界面/输入等事件处理器接线；
- 收枪渲染保留窗口及快速切换处理。

以上是版本修复范围，不是一份新造的逐用例日志。维护者的整体 PASS **不扩展为**：

- 所有第三方枪包及其所有版本均已验证；
- 与官方 Figura LuaJ fork 的所有边界行为完全等价；
- 每个光影/视角模组组合、LAN/专服或混合/代理/面板环境都完成新一轮矩阵；
- 发布工作流已完成上传，或最终 tag 的真实 jar 已通过新 L0 检查。

版本发布前仍按 [`RELEASE.md`](../publish/RELEASE.md) 的逐项清单核对，尤其是 LAN
第二名玩家完整进入世界与最终产物检查。未确认的专项不标为 PASS。

## 3. 文档与历史记录处理

- 同步 `gradle.properties`、README 顶部/版本导航/支持表/版本约束、CHANGELOG R3 条目、
  兼容矩阵、Release notes 与开发/发布操作说明。
- `RELEASE_NOTES.md` 首行改为
  `<!-- release-version: 1.1.8+neoforge.26.1.2.R3 -->`，不通过删门禁绕过草稿检查。
- 本轮不改游戏代码、加载器版本、依赖、mappings 或 modId。
- 上轮 [`Lua 移植记录`](SCRIPT_STRINGLIB_RESTORE_2612_20260906.md) 和
  [`发布工作流适配记录`](RELEASE_WORKFLOW_PORT_2612_20260907.md) 是当时快照，
  其“未获得本线实机确认 / 仍为 R2”等表述不倒改，由本记录追加当前确认。
- 正文按本线 R3 准备；**本轮只请求合并代码，不创建 tag 或 GitHub Release**，
  README 不添加尚不存在的 R3 发布链接。

## 4. 工作流上线状态已实际核对

维护者已自行添加 `.github/workflows/release.yml`：

- 默认分支提交
  [`54c4c21a`](https://github.com/q14433686-arch/TaCZ_Renovated/commit/54c4c21a5ef3a07114fafae5fc9b94ae6feb4a79)；
- 本工作分支提交
  [`0faa1e9a`](https://github.com/q14433686-arch/TaCZ_Renovated/commit/0faa1e9a007e40a0c948914f676fe9eff381d6cc)。

已逐字节确认两份正式件与 `docs/publish/ci/release.yml` 一致；GitHub Actions API 返回
`release` workflow（ID `351854275`）状态为 **active**。所以当前活文档改为“已由维护者
上线”，不再要求重复复制。**定义已上线与执行发布流程是两回事**；本轮没有运行 release。

为避免 PR 中重复呈现相同工作流，先将已核对的默认分支提交合入当前工作分支；始终在
`arena/01a078a6-tacz-renovated` 工作，未切换或直接推送默认分支。正式 workflow 及同源模板
均不修改，不再次触发 App 缺少 workflows 写权限的问题。

## 5. R3 本轮检查

- `bash scripts/check_release_consistency.sh --strict`：**通过**（README 四处、CHANGELOG 与署名门禁）。
- `python3 scripts/verify_release.py --tag 26.1.2_R3`：**通过**；这是源码/版本头与输入 tag
  字符串的预检，不证明远端 tag 已创建。旧 `26.1.2_R2` 输入按预期被拒绝。
- 17 项发布门禁 unittest、文档链接、现有工作流的 mixin/语言静态检查与 `git diff --check`：
  **通过**；正式 workflow 与模板的逐字节一致性也再次通过。
- R3 的 `compileJava` / 完整 build：待本次版本提交的 CI 结果，不用旧 R2 的成功冒充。
- 实机状态来源为 §1 的维护者确认；发布流程上传、最终 tag L0 与未单列的专项矩阵
  不写成已通过。

PR #47 在最终代码/文档检查通过后合并；合并结果以 GitHub PR 页面与本轮交接为准。
